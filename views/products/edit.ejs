<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Produce Stand CRUD</title>
</head>
<body>
<div class="alert alert-success">
  <strong>You</strong> should <a href="#" class="alert-link">EDIT DETAILS</a>.
</div>
<a href="/products">Cancel</a>
<p>Editing "<%= product.name %>"</p>
<form action="/products/<%= product.id %>?_method=PUT" method="POST">
   <p class="h6">Product name</p>
    <input type="text" name="name" id="product_name" value="<%= product.name %>">

   <div class="mt-4 p-5 bg-primary text-white rounded">
  <p class="h6">Product Price</p>
</div>
    <input type="text" name="price" id="product_price" value="<%= product.price %>">

    <label for="category">Category:</label>
    <select name="category" id="category">
            <% for (let category of categories) {%>
                <option value="<%= category %>" <%= product.category === category ? 'selected': '' %> ><%= category
                    %></option>
            <% } %>
    </select>
    <input type="submit" class="btn btn-success" value="Submit Button">

</form>
</body>
</html>
